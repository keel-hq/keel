# Implementation Tasks: Summarize the Keel Repository's Top-Level Directories

- [ ] Enumerate and alphabetize all tracked top-level directories in `keel`, excluding `.git`.
- [ ] Verify each brief summary against a README, manifest, package name, or representative file.
- [ ] Return the 25-directory summary as text without creating an artifact in either code repository.
- [ ] Confirm `keel` and `b-alex` have no new, modified, or deleted files.
