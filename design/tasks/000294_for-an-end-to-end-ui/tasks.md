# Implementation Tasks: Add Workspace Inspector E2E Fixture

- [ ] Confirm which local repository should contain `workspace-inspector-e2e/`.
- [ ] Create `workspace-inspector-e2e/alpha.ts` with an exported, explicitly typed numeric `add` function.
- [ ] Create `workspace-inspector-e2e/README.md` describing the fixture and `add` function.
- [ ] Verify both requested files exist at the expected paths.
- [ ] Verify the two files are uncommitted and do not commit or push them.
