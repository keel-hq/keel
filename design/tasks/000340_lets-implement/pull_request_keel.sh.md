# No changes for this task

## Summary
This task implements keel-hq/keel#852, a nightly pre-release GitHub Actions workflow. The user-facing documentation for the nightly images was added to `readme.md` in the `keel` repository, so the `keel.sh` website repository needs no changes.

## Testing
Not applicable — no code changes in this repository. `git status` is clean.
