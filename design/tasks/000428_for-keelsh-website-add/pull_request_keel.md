# chore: no changes required for keel website favicon task

## Summary
This task adds a favicon to the keel.sh website. All changes live in the `keel.sh` repository (favicon assets plus VuePress `head` config); the `keel` operator codebase requires no changes, so no feature branch was pushed here.

## Testing
Nothing to test in this repository — no code was modified.
